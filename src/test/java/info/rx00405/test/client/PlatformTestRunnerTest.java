/*
 * Copyright (c) 2024 Battle Road Consulting. All rights reserved.
 * 
 */

package info.rx00405.test.client;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
//import static org.junit.jupiter.api.Assertions.*;
//import java.util.List;

class PlatformTestRunnerTest {

    @Disabled("This test needs to be refactored to work with the new PlatformTestRunner")
    @Test
    void testRunAllTests() {
        // PlatformTestRunner runner = new PlatformTestRunner();
        // List<TestResult> results = runner.runAllTests();

        // assertNotNull(results);
        // assertFalse(results.isEmpty());
    }

    @Disabled("This test needs to be refactored to work with the new PlatformTestRunner")
    @Test
    void testRunTestByName() {
        // PlatformTestRunner runner = new PlatformTestRunner();
        // TestResult result = runner.runTestByName("sampleTest");

        // assertNotNull(result);
        // assertEquals("sampleTest", result.getTestName());
    }

    @Disabled("This test needs to be refactored to work with the new PlatformTestRunner")
    @Test
    void testRunTestByNameWithInvalidName() {
        // PlatformTestRunner runner = new PlatformTestRunner();
        // TestResult result = runner.runTestByName("invalidTest");

        // assertNull(result);
    }

    @Disabled("This test needs to be refactored to work with the new PlatformTestRunner")
    @Test
    void testGetTestNames() {
        // PlatformTestRunner runner = new PlatformTestRunner();
        // List<String> testNames = runner.getTestNames();

        // assertNotNull(testNames);
        // assertTrue(testNames.contains("sampleTest"));
    }
}